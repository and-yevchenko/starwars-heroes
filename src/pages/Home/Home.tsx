import { HeroesList } from '../../components/HeroesList/HeroesList';
import './Home.css';

export const Home = () => {
    return (
        <main className="home">
            <HeroesList />
        </main>
    );
};
